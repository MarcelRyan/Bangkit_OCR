version: "3.8"

services:
  fastapi-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastapi-app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - APP_NAME=OCR + GenAI App
      - OCR_MODEL_PATH=./models
      - DEBUG=False
